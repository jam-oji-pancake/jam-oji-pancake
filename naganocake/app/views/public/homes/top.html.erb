<div class="container">
  <div class="row">

  <div class='col-3'>
    <table classs="table table-hover">
      <thead>
        <tr>
          <th>ジャンル</th>
        </tr>
      </thead>

      <tbody>
        <% @genres.each do |genre| %>
          <tr>
            <td>
              <%= genre.name %>
            <td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>

  <div class='col-9'>
    <div class="example">
    <%= image_tag 'bread_panya.png',:size =>'800x400' %>
    <p>
      </br>
      ようこそ、ながのCAKEへ！</br>
      このサイト、はケーキ販売のECサイトになります。　　</br>
      会員でない方も商品の閲覧は出来ますが、</br>
      購入には会員登録が必要です。</br>
      </br>
    </p>
    </div>
    
    <h2>新着商品</h2><h4>全<%= @all_items.count %>件</h4>
    <div class="d-flex flex-wrap">
    <% @items.each do |item| %>
      <div>
        <%= link_to item_path(item.id) do %>
        <%= attachment_image_tag(item, :image, :fill, 100, 100, fallback: "no_image.jpg") %>
        <p><%= item.name %></p>
        <% end %>
        <%= item.add_tax_price.to_s(:delimited) %>円
        <!--%= item.add_tax_price.to_s(:delimited) %> 円-->
      </div>
    <% end %>
      <div>
        <%= link_to '全ての商品を見る', items_path %>
      </div>
    </div>
  </div>
  </div>
</div>
<%= link_to 'カスタマーshowへ', mypage_path,class: "btn btn-outline-secondary text-dark" %>
<%= link_to 'カスタマーeditへ', mypage_edit_path,class: "btn btn-outline-secondary text-dark" %>
<%= link_to '管理者側ログイン画面へ', new_admin_session_path,class: "btn btn-outline-secondary text-dark" %>